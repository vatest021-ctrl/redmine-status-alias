# Redmine Status Alias

Плагин для Redmine 6.0.x, который показывает клиентам выбранные внешние названия статусов обращений без изменения внутренних статусов, workflow и истории задач.

## Назначение

В технической поддержке внутренние статусы Redmine часто слишком детальны для клиентов. `redmine_status_alias` позволяет администратору выбрать роли клиентов и настроить соответствие:

- внутренний статус Redmine;
- статус, который должен видеть клиент.

Например, внутренний статус `Диагностика у 2 линии` можно показывать клиенту как `В работе`.

## Совместимость

- Redmine: `6.0.0+`, проверочный целевой стенд: `6.0.5`
- Ruby/Rails: используются версии из установленного Redmine
- Миграции БД: не требуются

## Установка

Скопируйте или клонируйте репозиторий в каталог плагинов Redmine:

```bash
cd /path/to/redmine/plugins
git clone git@github.com:vatest021-ctrl/redmine-status-alias.git redmine_status_alias
```

Перезапустите Redmine. Миграции можно не запускать, но безопасно выполнить стандартную команду:

```bash
cd /path/to/redmine
bundle exec rails redmine:plugins:migrate RAILS_ENV=production
```

## Настройка

Откройте:

```text
Администрирование -> Модули -> Redmine Status Alias -> Настроить
```

Далее:

1. выберите роли клиентов;
2. при необходимости выберите служебные роли, которые всегда должны видеть внутренние статусы;
3. задайте соответствие внутренних статусов статусам, видимым клиенту;
4. сохраните настройки.

Затем включите модуль только в нужных проектах:

```text
Проект -> Настройки -> Модули -> Клиентские алиасы статусов
```

Администраторы всегда видят исходные внутренние статусы.

## Как это работает

Плагин регистрирует проектный модуль `status_alias`. Alias-статусы применяются только для задач тех проектов, где этот модуль включен.

Плагин подключает небольшие patch'и к основным точкам Redmine, где отображаются статусы. `Issue#status` передает проектный контекст, а `IssueStatus#name` возвращает alias только если:

- в проекте включен модуль `Клиентские алиасы статусов`;
- текущий пользователь имеет одну из выбранных клиентских ролей именно в этом проекте;
- у текущего пользователя нет выбранной служебной роли в этом проекте;
- для внутреннего статуса задан alias.

ID статуса задачи не меняется.

## Что покрыто

- карточка задачи;
- список задач и колонки запросов;
- формы редактирования, массового редактирования и контекстное меню смены статуса;
- фильтры по статусу;
- история изменений статуса;
- email-уведомления по задачам;
- проектные отчеты по задачам;
- CSV/PDF-выгрузки, которые используют стандартные query columns Redmine.

Клиент выбирает в форме реальные статусы Redmine, но видит их клиентские названия. Workflow Redmine остается источником правды: если клиент может менять статус, это должно быть разрешено стандартными правилами workflow для его роли.

Это означает:

- workflow Redmine не меняется;
- отчеты и фильтры продолжают работать по исходным статусам;
- история задач остается технически корректной;
- клиентский интерфейс показывает более понятные статусы.

## Важно

Глобальные настройки плагина задают только роли и соответствия статусов. Само применение включается отдельно в каждом проекте через стандартную вкладку модулей Redmine.

Если несколько внутренних статусов имеют один и тот же клиентский alias, в списках это нормально. Но в формах смены статуса клиент может увидеть несколько одинаковых пунктов, потому что за ними стоят разные реальные `status_id`. Для клиентских workflow лучше оставлять только однозначные переходы.
